# 目录结构
.idea:保存项目的配置信息
.venv:虚拟环境，保存项目的环境信息
>ps:通过虚拟的运行环境.venv,可以保证每一个python项目都在一个相对独立的隔离的运行环境中运行，项目与项目之间互不影响。

>python是动态类型语言，添加类型注解只是提示，并不会强制执行类型检查，类型注解主要是为了提高代码的可读性和可维护性，帮助开发者更好地理解代码的意图和数据结构。而不能够强制执行类型检查是因为python的动态特性，允许在运行时改变变量的类型，这也是python灵活性的一个重要特点。